import {SafeAreaView, ScrollView} from "react-native";
import ListRegions from "../../../features/Customer/ListRegions";

const ListRegionsScreen = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <ListRegions/>
            </ScrollView>
        </SafeAreaView>
    )
}

export default ListRegionsScreen;
